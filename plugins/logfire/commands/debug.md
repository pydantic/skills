---
name: debug
description: Use Logfire traces to investigate errors and debug production issues
---

# /debug

Load and follow the `logfire-query` skill for evidence-backed investigation.
If the user wants a browser view or link rather than analysis in chat, use the
`logfire-ui` skill instead. The user's instructions take precedence over both
skills.
