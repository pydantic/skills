{
  "skill_name": "migrating-langchain-to-pydantic-ai",
  "evals": [
    {
      "id": 1,
      "prompt": "Migrate evals/files/simple_agent.py from LangChain to native Pydantic AI. Preserve its authenticated-customer boundary, structured result, dynamic locale instructions, and approval requirement for refunds. Write migrated_agent.py and test_migrated_agent.py without making a live model request.",
      "expected_output": "A runnable Pydantic AI vertical slice and deterministic tests that preserve output, authorization, dynamic instructions, and deferred approval behavior without retaining LangChain imports.",
      "files": [
        "evals/files/simple_agent.py"
      ],
      "assertions": [
        "migrated_agent.py contains no LangChain or LangGraph imports",
        "Authenticated customer identity and the order service are typed dependencies rather than model-chosen tool arguments",
        "The terminal result is represented by a typed Pydantic output model",
        "Locale-dependent instructions are derived from RunContext",
        "Tests capture model requests and prove locale-dependent instructions change for at least two dependency values",
        "Refund approval uses Pydantic AI deferred tool approval rather than prompt-only authorization",
        "Tests exercise authenticated identity propagation and concealment plus typed output without a live provider call",
        "Tests prove a denied refund never calls the service and an approved resume calls it exactly once with the original authenticated customer and order"
      ]
    },
    {
      "id": 2,
      "prompt": "Audit evals/files/persistent_graph.py and write a migration ledger plus an incremental Pydantic AI migration plan. Do not implement it. The current workflow must survive process restarts and must never charge an order twice after replay.",
      "expected_output": "An evidence-based plan that separates conversation history, workflow state, approval correlation, and side-effect recovery instead of treating message history as a LangGraph checkpoint replacement.",
      "files": [
        "evals/files/persistent_graph.py"
      ],
      "assertions": [
        "The ledger accounts for every state field, node, interrupt, checkpointer, and external charge side effect",
        "The plan explicitly states that Pydantic AI message history is not a full LangGraph checkpoint",
        "Approval correlation and durable resume are owned by an application or durable workflow boundary",
        "The charge operation retains an idempotency key and defines replay behavior for an unknown-after-crash outcome",
        "Pydantic message continuation is not described as restoring full graph or workflow state",
        "The first migration slice is reversible and has concrete parity tests"
      ]
    }
  ]
}
