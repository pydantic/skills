# ai

Build production-grade AI agents with [Pydantic AI](https://ai.pydantic.dev/) and migrate existing LangChain systems.

## Features

- SKILL.md with core Pydantic AI patterns (agents, tools, capabilities, structured output, testing, multi-agent systems)
- Reference docs for common tasks (hooks, specs, streaming, fallback models, delegation)
- Migration workflow, concept mappings, project-scale studies, verification guidance, and a LangChain inventory script

## Install

```bash
claude plugin marketplace add pydantic/skills
claude plugin install ai@pydantic-skills
```

## Codex

`ai` is not currently listed as a Codex plugin in the Pydantic marketplace. Codex users can still use the
standalone cross-agent skills at `skills/building-pydantic-ai-agents/` and
`skills/migrating-langchain-to-pydantic-ai/`.
